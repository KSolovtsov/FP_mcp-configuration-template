# Slack Configuration
# Slack browser tokens (Stealth Mode)

# Browser Token (xoxc-...)
# How to get:
# 1. Open Slack in browser (Chrome/Firefox)
# 2. Open DevTools (F12)
# 3. Go to Network tab
# 4. Refresh page
# 5. Find any request to api.slack.com
# 6. Look in Headers for "Authorization: Bearer xoxc-..."
SLACK_MCP_XOXC_TOKEN=xoxc-your-token-here

# Browser Cookie (xoxd-...)
# From the same request:
# 7. Look in Headers for "Cookie: d=xoxd-..."
SLACK_MCP_XOXD_TOKEN=xoxd-your-cookie-here

# Alternative: OAuth User Token (xoxp-...)
# If you have an OAuth token, you can use it instead of xoxc/xoxd
# SLACK_MCP_XOXP_TOKEN=xoxp-your-oauth-token-here

# Server Configuration
SLACK_MCP_PORT=13080
SLACK_MCP_HOST=127.0.0.1

# Cache Files (improves performance)
SLACK_MCP_USERS_CACHE=.users_cache.json
SLACK_MCP_CHANNELS_CACHE=.channels_cache_v2.json

# Log Level (debug, info, warn, error)
SLACK_MCP_LOG_LEVEL=info

# Message Posting (disabled by default for safety)
# Options:
#   false - disabled (default)
#   true  - enabled for all channels
#   C123,C456 - enabled only for specific channels
#   !C123 - enabled for all except C123
SLACK_MCP_ADD_MESSAGE_TOOL=false

# Optional: Proxy
# SLACK_MCP_PROXY=http://proxy.company.com:8080

# Optional: Enterprise Slack
# SLACK_MCP_USER_AGENT=Mozilla/5.0 ...
# SLACK_MCP_CUSTOM_TLS=true
